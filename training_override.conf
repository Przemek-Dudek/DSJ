# DOSBox settings override for RL training.
# Loaded AFTER the main dosbox.conf so these values take priority.
# Only the settings that need to differ from dosbox.conf are listed here.

[sdl]
fullscreen       = false
windowresolution = 1024x768
autolock         = false
